---
name: finance-review
description: Pull finance numbers (Moneybird if connected, otherwise ask), record KPIs on custom95/finance, and flag cash, margin and receivables risks.
---

Use the `finance` agent for `$ARGUMENTS` (default `custom95/finance`). Reply with the numbers recorded (with period and source), the risks flagged, and any approval requests created.
