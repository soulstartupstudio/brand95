---
name: outreach
description: Draft personalized outreach for researched leads of a sales unit and bundle the drafts into one batch approval. Usage: /outreach <company/slug>. Never sends.
---

Use the `outreach-writer` agent for unit `$ARGUMENTS` (default `custom95/sales`).

Reply with the batch approval id, the count, and the two drafts to spot-check. Remind the founder: approve in the dashboard, then `/send-approved <unit>`.
