---
name: ops-review
description: Weekly operating review for an operations department: exceptions, SOP gaps, automation health, founder hours. Creates owned tasks. Usage: /ops-review <company>/operations.
---

Use the `ops` agent for `$ARGUMENTS` (default `custom95/operations`). Reply with the failing health checks, the tasks created (owner, due) and the note id of the report.
