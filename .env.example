# Jarvis Command Center
# Path to the SQLite database (created on `jarvis init`)
JARVIS_DB=./data/jarvis.db
# Dashboard port for `jarvis serve`
JARVIS_PORT=4795
# Founder identity used in the audit log
JARVIS_FOUNDER=dex
